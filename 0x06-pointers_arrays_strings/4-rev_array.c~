/**
 * reverse_array (int *a, int n);
 * @a: array
 * @n: size
  */
void reverse_array(int *a, int n)
{
	int start = 0, end = n, tmp;
	for (;start < end && start < n, start++, end--)
	{
		tmp = a[i];
		a[i] = a[end];
		a[end] = tmp;
	}
}
