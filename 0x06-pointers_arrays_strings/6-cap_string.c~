#include "holberton.h"
#include <stdio.h>

/**
 * cap_string - Transform first Letter to Uppercase
 * @s: string to transform
 * Return: char
 */
char *cap_string(char *s)
{
	separators = ",;.!?\"(){}"
	int i = 0;

	for (; s[i] ; i++){
		if(s[i] == ' ')
			printf(s[i]);
	}
	return ;
}
