/**
 * _strlen_recursion - find length of the string and return this length.
 * @s: string for find length
 * Return: length of string s  */
int _strlen_recursion(char *s)
{
	int length = 0;
	if (*s == '\0')
		return length;

	return 1 + _strlen_recursion(s + length)
}
