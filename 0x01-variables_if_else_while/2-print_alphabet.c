#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - last digit
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (char i = 'a'; i < 'z'; i++)
	{
		printf("%c", i);
	}
	printf("\n");
	return (0);
}
