#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Print the alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (char a = 'a'; a <= 'z' ; a++)
		putchar(a);
	printf("\n");
	return (0);
}
